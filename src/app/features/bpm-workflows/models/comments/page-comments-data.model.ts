export class PageCommentsData {
    NumPage?: number;
    NumSize?: number;


    constructor(NumPage: number, NumSize: number) {
        this.NumPage = NumPage;
        this.NumSize = NumSize;
    }
}
