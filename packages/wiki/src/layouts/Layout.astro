---
import '@/styles/globals.css';
import Sidebar from '@/components/Sidebar.astro';
import Header from './Header.astro';

interface Props {
  title: string;
}
const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="Manual de usuario para el sistema Sismas"
    />
    <meta
      name="viewport"
      content="width=device-width"
    />
    <link
      rel="icon"
      type="image/png"
      href="/wiki/Sismas_Favicon.png"
    />
    <meta
      name="generator"
      content={Astro.generator}
    />
    <script is:inline>
      // Immediate theme application to prevent FOUC
      const getTheme = () => {
        if (
          typeof localStorage !== 'undefined' &&
          localStorage.getItem('theme')
        ) {
          return localStorage.getItem('theme');
        }
        if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
          return 'dark';
        }
        return 'light';
      };
      const theme = getTheme();
      if (theme === 'dark') {
        document.documentElement.classList.add('dark');
      } else {
        document.documentElement.classList.remove('dark');
      }
    </script>
    <title>{title}</title>
  </head>
  <body
    class="flex min-h-screen bg-background font-sans antialiased text-foreground"
  >
    <Sidebar />
    <!-- línea 29: Un div que contendrá la cabecera y el contenido principal. Crece para ocupar el espacio disponible. -->
    <div class="flex flex-1 flex-col">
      <Header />
      <!-- línea 31: La cabecera de la página. -->

      <!-- línea 99: El contenedor principal del contenido de la página. -->
      <main class="flex-1 p-6">
        <!-- línea 101: ¡Esta es la parte más importante de un layout! -->
        <!-- '<slot />' es donde Astro inyectará el contenido de la página que está usando este layout. -->
        <slot />
      </main>
    </div>
  </body>
</html>
